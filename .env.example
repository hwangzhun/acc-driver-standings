# 管理员密码（必填，否则服务启动时会生成临时密码并打印到终端）
ADMIN_PASSWORD=1111

# 服务端口（默认 5174）
PORT=5174

# SQLite 数据库路径（默认 data/standings.sqlite）
# SQLITE_PATH=/path/to/standings.sqlite

# API base（前端构建时使用，Vite dev 代理到本服务，无需修改）
# VITE_API_BASE=